/**
 * @description authenticated event must be triggered to init
 * the plugin sequence.
 *
 */
export const AUTHENTICATED: string = 'authenticated'

/**
 * @description register opened event must be triggered to take
 * sub-sequent actions in store
 */
export const REGISTER_OPENED: string = 'register-opened'
